<div id="album_heading">
  <h1>Welcome to your album <%= @user.name %></h1>
</div>
<div id="image_form">
  <%= render "form" %>
  </div>
  <div id="album_notes">
    <p>Please note that, whenever you upload or add image from external url or your computer, you can edit it, but you won't be able to save it in current version of the programm</p>
    <p>If you're going to edit existing image, please remember it's name and enter it later to overwrite it.</p>
  </div>
<div id="album_table">
  <ul>
      <% @images.each do |img| %>
      <% unless img.image_url.empty? %>
          <li><p><%= img.name %></p><%= link_to image_tag(img.image_url), user_image_path(@user, img) %></li>
      <% else %>
        <li><p><%= img.name %></p><%= link_to image_tag(img.attachment), user_image_path(@user, img) %></li>
        <% end %>
      <% end %>
  </ul>
</div>